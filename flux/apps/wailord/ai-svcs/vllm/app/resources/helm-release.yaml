apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vllm
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: vllm
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    global:
      alwaysAppendIdentifierToResourceName: true
    controllers:
      main:
        containers:
          main:
            image:
              repository: vllm/vllm-openai-xpu
              tag: v0.26.0
              pullPolicy: IfNotPresent
            env:
              NEOReadDebugKeys: "1"
              OverrideGpuAddressSpace: "48"
              EnableImplicitScaling: "1"
              HF_API_KEY:
                secretKeyRef:
                  name: huggingface
                  key: token
              AUTH_API_KEY:
                secretKeyRef:
                  name: auth-token
                  key: token
              MODEL: "Wondernutts/gemma-4-26B-A4B-it-qat-q4_0-unquantized-uncensored-heretic-int4-ov"
            command: ["/bin/sh", "-c"]
            args: [
                "vllm serve $MODEL --trust-remote-code --enable-chunked-prefill --max-num-batched-tokens 1024"
            ]
            securityContext:
              privileged: true



    defaultPodOptions:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - bulk-datastore

    service:
      main:
        controller: main
        enabled: true
        type: LoadBalancer
        ports:
          http:
            port: 8000

    route:
      main:
        kind: HTTPRoute
        parentRefs:
          - name: "internal"
            namespace: "gateway-svcs"
            sectionName: websecure-gw
        hostnames:
          - "ai.jsck.network"
        rules:
          - name: "default"
            backendRefs:
              - identifier: main
                port: 8800
            matches:
              - path:
                  value: /
                  type: PathPrefix

    persistence:
      config:
        enabled: false
        type: emptyDir

      model-cache:
        enabled: true
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 100Gi
        advancedMounts:
          main:
            main:
              - path: /root/.cache/huggingface