apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: veymax-backup-target
  labels:
    backup-user: &username veymax
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: veymax-backup-target
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    global:
      alwaysAppendIdentifierToResourceName: true
    controllers:
      main:
        enabled: true
        strategy: Recreate
        containers:
          main:
            image:
              repository: &backup_img ghcr.io/onemorebyte/backup-target
              tag: &backup_tag 0.2.2
              pullPolicy: IfNotPresent
            env:
              TZ: America/Chicago
              OTHER: test
            securityContext:
              privileged: true
              fsGroup: 20422
        initContainers:
          create-host-keys:
            image:
              repository: *backup_img
              tag: *backup_tag
              pullPolicy: IfNotPresent
            command: ["bash", "-c", "ssh-keygen -A"]
          init-permissions:
            image:
              repository: *backup_img
              tag: *backup_tag
              pullPolicy: IfNotPresent
            command:
              - /usr/local/bin/configure-user.sh
              - *username
            securityContext:
              privileged: true

    defaultPodOptions:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - bulk-datastore

    service:
      main:
        controller: main
        ports:
          http:
            port: 22
    ingress:
      main:
        enabled: false

    configMaps:
      ssh-config:
        enabled: true
        data:
          authorized_keys: |
            ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKUO6bPf+7vFzN6cVbUwtT2Cc0YpkUCWZzUHSYlAo0Zx jsck@doubledown.jsck.network

    persistence:
      config:
        enabled: false
        type: emptyDir

      ssh:
        enabled: true
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 1Gi
        advancedMounts:
          main:
            create-host-keys:
              - path: /etc/ssh
            main:
              - path: /ssh
                readOnly: true

      backup-dataset:
        enabled: true
        type: hostPath
        hostPath: &backup_dir /var/mnt/store/backups/foreign/veymax
        advancedMounts:
          main:
            main:
              - path: *backup_dir

      home:
        enabled: true
        type: hostPath
        hostPath: *backup_dir
        advancedMounts:
          main:
            main:
              - path: /home/veymax/
            init-permissions:
              - path: /home/veymax/

      home-ssh:
        enabled: true
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /home/veymax/.ssh
            init-permissions:
              - path: /home/veymax/.ssh

      home-ssh-keys:
        enabled: true
        type: configMap
        identifier: ssh-config
        defaultMode: 0600
        advancedMounts:
          main:
            main:
              - path: /home/veymax/.ssh/authorized_keys
                subPath: authorized_keys
                readOnly: true
            init-permissions:
              - path: /home/veymax/.ssh/authorized_keys
                subPath: authorized_keys
