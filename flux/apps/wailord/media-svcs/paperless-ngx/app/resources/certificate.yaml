apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paper.jackhil.de-cert
spec:
  dnsNames:
    - "paper.jackhil.de"
  secretName: paper.jackhil.de-cert
  issuerRef:
    name: letsencrypt-cloudflare-issuer
    kind: ClusterIssuer
