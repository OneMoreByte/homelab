apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: readarr.jackhil.de-cert
spec:
  dnsNames:
    - "readarr.jackhil.de"
  secretName: readarr.jackhil.de-cert
  issuerRef:
    name: letsencrypt-cloudflare-issuer
    kind: ClusterIssuer
