apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flood.jackhil.de-cert
spec:
  dnsNames:
    - "flood.jackhil.de"
  secretName: flood.jackhil.de-cert
  issuerRef:
    name: letsencrypt-cloudflare-issuer
    kind: ClusterIssuer
