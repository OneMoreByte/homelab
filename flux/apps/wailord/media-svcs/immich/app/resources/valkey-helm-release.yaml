apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich-valkey
spec:
  interval: 1h
  chart:
    spec:
      chart: immich-valkey
      version:  0.7.2
      sourceRef:
        kind: HelmRepository
        name: immich-valkey
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values: {}
