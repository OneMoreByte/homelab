apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: photos.jackhil.de-cert
spec:
  dnsNames:
    - "photos.jackhil.de"
  secretName: photos.jackhil.de-cert
  issuerRef:
    name: letsencrypt-cloudflare-issuer
    kind: ClusterIssuer
