apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: plex.jackhil.de-cert
spec:
  dnsNames:
    - "plex.jackhil.de"
  secretName: plex.jackhil.de-cert
  issuerRef:
    name: letsencrypt-cloudflare-issuer
    kind: ClusterIssuer
