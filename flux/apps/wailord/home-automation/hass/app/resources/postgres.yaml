apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: hass-pg-cluster
spec:
  instances: 1
  bootstrap:
    initdb:
      database: hass
      owner: hass
      secret:
        name: hass-db-secret
  storage:
    size: 128Gi
