apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: spoolman-pg-cluster
spec:
  instances: 1
  bootstrap:
    initdb:
      database: spoolman
      owner: spoolman
      secret:
        name: spoolman-db-secret
  storage:
    size: 40Gi
