apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: seerr-pg-cluster
spec:
  instances: 1
  bootstrap:
    initdb:
      database: seerr
      owner: seerr
      secret:
        name: seerr-db-secret
  storage:
    size: 40Gi
