apiVersion: v1
kind: Service
metadata:
  name: {{ include "rom-sftp.fullname" . }}
  labels:
    {{- include "rom-sftp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rom-sftp.selectorLabels" . | nindent 4 }}
