apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mva.fullname" . }}-config
spec:
  accessModes:
    - ReadWriteOnce
#  storageClassName: fast
  resources:
    requests:
      storage: 30Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mva.fullname" . }}-data
spec:
  accessModes:
    - ReadWriteOnce
#  storageClassName: fast
  resources:
    requests:
      storage: 30Gi