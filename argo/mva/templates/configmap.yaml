apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mva.fullname" . }}-configs
data:
  # Config for mva
  mva.yml: |
    {{ toYaml .Values.mva_config | nindent 4  }}
  # Config for flexget
  config.yml: | 
    {{ toYaml .Values.flexget_config | nindent 4 }}