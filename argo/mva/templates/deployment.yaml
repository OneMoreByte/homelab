apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mva.fullname" . }}
  labels:
    {{- include "mva.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "mva.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "mva.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "mva.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          volumeMounts:
          - name: media-dir
            mountPath: /plex-files
          - name: metadata-dir
            mountPath: /metadata
          - name: torrent-tmpdir
            mountPath: /mva
          - name: config-yaml
            mountPath: /config-yaml
          - name: config
            mountPath: /config
          - name: data
            mountPath: /data  
          command: ["sh"]
          args: ['-c', 'ln -s /config-yaml/config.yaml /config/config.yaml  && flexget daemon start']
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: media-dir
          hostPath:
            path: /store/plex-files
            type: Directory
        - name: metadata-dir
          hostPath:
            path: /store/meta
            type: Directory
        - name: torrent-tmpdir
          emptyDir: {}
        - name: config-yaml
          configMap:
            name: {{ include "mva.fullname" . }}-configs
        - name: config
          persistentVolumeClaim:
            claimName: {{ include "mva.fullname" . }}-config-pvc
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "mva.fullname" . }}-data-pvc
