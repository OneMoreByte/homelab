tailscale-operator:
  oauth: {}

  operatorConfig:
    image:
      repo: ghcr.io/onemorebyte/tailscale
      tag: 1.62.1-operator
      pullPolicy: "Always"
    hostname: "wailord"
  proxyConfig:
    image:
      repo: ghcr.io/onemorebyte/tailscale
      tag: 1.62.1-client
      pullPolicy: "Always"
    defaultTags: "tag:k8s,tag:wailord"

  apiServerProxyConfig:
    mode: "true"
