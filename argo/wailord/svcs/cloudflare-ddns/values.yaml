app-template:
  controllers:
    main:
      enabled: true
      strategy: Recreate
      containers:
        main:
          env:
            CONFIG_PATH: /config
  service:
    main:
      ports:
        http:
          port: 9999
  image:
    repository: timothyjmiller/cloudflare-ddns
    tag: "latest"
    pullPolicy: Always


  persistence:
    config:
      enabled: true
      type: secret
      name: cloudflare-ddns-config
