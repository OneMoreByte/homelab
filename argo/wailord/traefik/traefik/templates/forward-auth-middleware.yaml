apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: forward-auth-middleware
spec:
  forwardAuth:
    address: http://traefik-traefik-forward-auth:4181
    authResponseHeaders:
      - "X-Forwarded-User"
