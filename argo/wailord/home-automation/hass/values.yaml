app-template:

  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/home-assistant/home-assistant
            tag: latest@sha256:515d16b46f1e502ba415a9333b411269845dc1b29f4f27ed10fa91d1d2d319ba
            pullPolicy: IfNotPresent

          env:
            # -- Set the container timezone
            TZ: "America/Chicago"

  service:
    main:
      controller: main
      ports:
        http:
          port: 8123

  ingress:
    main:
      enabled: true
      annotations:
        traefik.ingress.kubernetes.io/router.tls: "true"
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
      labels: {}
      hosts:
        - host: home.jackhil.de
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - home.jackhil.de
          secretName: jackhil.de-cert

  persistence:
    config:
      enabled: true
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 12Gi
