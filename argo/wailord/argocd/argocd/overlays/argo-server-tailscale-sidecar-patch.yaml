apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  template:
    spec:
      containers:
        - name: ts-sidecar
          imagePullPolicy: Always
          image: "ghcr.io/tailscale/tailscale:latest"
          env:
            - name: TS_KUBE_SECRET
              value: "argo-tailscale-state-server"
            - name: TS_USERSPACE
              value: "false"
            - name: TS_AUTHKEY
              valueFrom:
                secretKeyRef:
                  name: argo-tailscale-auth
                  key: TS_AUTHKEY
                  optional: true
          securityContext:
            privileged: true
            capabilities:
              add:
                - NET_ADMIN
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 169.254.25.10
          - 100.100.100.100
        searches:
          - argocd.svc.cluster.local
          - svc.cluster.local
          - default.svc.cluster.local
          - fawn-mamba.ts.net
          - cluster.local
        options:
          - name: ndots
            value: "5"
