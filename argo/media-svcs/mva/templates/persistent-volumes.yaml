apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mva.fullname" . }}-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
#  storageClassName: fast
  resources:
    requests:
      storage: 5Gi
